```js
//调用方法解析

// 获取balance之类的
getAllowance(currentAccount.address, window.racaAddress, sale_address)

// 获取approve state
E = u.sent
getApprovedState:  E - (m == null ? void 0 : m.total_price) >= 0

// approve操作
J.approve(window.racaAddress, pe, l.sale_address, J.parseUnits(m.toString()));
//p.s:  pe是一个方法
pe = function(c) {
  var l;
  G && G((l = Oe.current) === null || l === void 0 ? void 0 : l.input.value, c)
}

拍卖合约地址： 0x7b4452dd6c38597fa9364ac8905c27ea44425832   abi未公开